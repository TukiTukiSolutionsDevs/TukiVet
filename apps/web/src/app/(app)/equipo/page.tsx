import { PlaceholderScreen } from "@/components/shell/placeholder-screen";

export default function Page() {
  return (
    <PlaceholderScreen
      title="Equipo"
      description="Usuarios, roles RBAC (5 roles, 41 permisos) y auditoría."
    />
  );
}
